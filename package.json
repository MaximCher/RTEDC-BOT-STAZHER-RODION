{
  "name": "srvt-assistant-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "import:subsidies": "ts-node-dev --transpile-only src/scripts/importSubsidiesFromFile.ts",
    "srvt:ingest": "ts-node-dev --transpile-only scripts/srvt-ingest.ts",
    "manual:ingest": "ts-node-dev --transpile-only scripts/manual-ingest.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "@supabase/supabase-js": "^2.87.0",
    "axios": "^1.13.2",
    "cheerio": "^1.1.2",
    "dotenv": "^16.4.5",
    "openai": "^4.78.1",
    "pg": "^8.12.0",
    "telegraf": "^4.16.3",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "prisma": "^6.1.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
